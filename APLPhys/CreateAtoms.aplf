 CreateAtoms(n posopt velopt)
 natoms←n
 density←natoms÷volume
 :If posopt∊random
     pos←?natoms dim⍴0 ⍝ Generate random positions
 :EndIf
 :If velopt∊random
     vel←?natoms dim⍴0
     ⍝ Remove CoM drift
     vel←vel-⍤1⊢(+⌿÷≢)vel
 :EndIf
