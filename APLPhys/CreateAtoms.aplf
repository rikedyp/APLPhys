 CreateAtoms args
 natoms pos vel←args
 density←natoms÷volume
 :If 'random'≡⊃⊆pos
     ⎕RL←{3 4::⍬ ⋄ 2⊃⍵}⊆pos
     pos←?natoms dim⍴0
     ⍝ Remove centre-of-mass drift
     pos←pos-⍤1⊢(+⌿÷≢)pos
 :EndIf
 :If 'random'≡⊃⊆vel
     ⎕RL←{3 4::⍬ ⋄ 2⊃⍵}⊆vel
     vel←¯0.5+?natoms dim⍴0
 :ElseIf 0≡2⊃args
     vel←natoms dim⍴0
 :EndIf
