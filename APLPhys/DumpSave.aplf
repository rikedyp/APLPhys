 filenames DumpSave(steps dumps);folder

 :For d :In ⍳≢dumps
     :If (⊃⌽(folder←'.'(≠⊆⊢)⊃filenames[d]))≡'csv'
         :Trap 22
             ⎕MKDIR folder←'.',⊃⊃{(~'.csv'⍷⍵)⊆⍵}folder

             :For i :In ⍳steps÷dumpfreqs[d]
                 (⊃(⊃log[+/(⍳≢lookup)×dumps[d]⍷lookup])[i])⎕CSV folder,'/',(⍕i),'.csv'
             :EndFor
         :EndTrap
     :Else

         (⊂↓{⍕⍪,((⊂'step:'),¨dumpfreqs[d]×⍳⍴⍵),[1.5]⍵}⊃log[+/(⍳≢lookup)×dumps[d]⍷lookup])⎕NPUT filenames[d],1

     :EndIf
 :EndFor
