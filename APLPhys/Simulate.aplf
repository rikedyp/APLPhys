 Simulate nsteps;i
 (acc ene_pot_avg virial)←ComputeForces pos
 :For i :In ⍳nsteps
     log←ComputeForces RunStyle pos vel acc
     (pos vel acc)←3↑log
   ⍝  {0=100|i:∘∘∘}⍬
     ⍝ TODO: Seems bad to execute DumpText even if no dump set in input file
     (⊂dumpfiles[d])DumpText i,⊂⍎¨dumps[d←⍸0=dumpfreqs|i]
 :EndFor
