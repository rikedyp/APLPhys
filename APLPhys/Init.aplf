 {r}←Init
 ⍝ Names that script files can use
 pos←⍬
 ⍝ Initial values
 random←⊂'random'
 hexagonal←⊂'hexagonal'
 ⍝ Default values
 Thermostat←{⍵}
 neighfreq←1
 ⍝ Output dump options
 dumps dumpfreqs dumpfiles←⍬ ⍬ ⍬
