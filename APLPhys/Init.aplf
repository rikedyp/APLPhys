 {r}←Init
 ⍝ Names that script files can use
 groups←⍬
 fixgroups←⍬
 allforce←0
 walls←⍬
 ⍝ Initial values
 random←⊂'random'
 hexagonal←⊂'hexagonal'
 ⍝ Default values
 Thermostat←{⍵}
 ComputeWall←{⍵}
 mass←1
 ⍝ Output dump options
 dumps dumpfreqs dumpfiles←⍬ ⍬ ⍬
