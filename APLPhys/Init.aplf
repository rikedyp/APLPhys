 {r}←Init
 ⍝ Names that script files can use
 pos←0 2⍴⍬
 ⍝ Initial values
 random←⊂'random'
 hexagonal←⊂'hexagonal'
 ⍝ Default values
 Thermostat←{⍵}
 ⍝ Output dump options
 dumps dumpfreqs dumpfiles←⍬ ⍬ ⍬
