 {r}←Init
 ⍝ Names that script files can use
 ⍝ natoms←0
 groups←⍬
 fixgroups←⍬
 allforce←{}
⍝ pos←0 dim⍴⍬
 ⍝ Initial values
 random←⊂'random'
 hexagonal←⊂'hexagonal'
 ⍝ Default values
 Thermostat←{⍵}
 ⍝ Output dump options
 dumps dumpfreqs dumpfiles←⍬ ⍬ ⍬
